package view;

import model.BaseModel;

// interface definition for the View classes

public interface ViewMVC {
	void updateCreate(BaseModel modelObject);	// newly created vehicles
	void updateDelete(BaseModel modelObject);	// deleted vehicles
	void updateMove(BaseModel modelObject);		// vehicles in motion
	
}